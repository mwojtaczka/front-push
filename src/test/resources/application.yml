spring:
  application:
    name: front-push
  kafka:
    bootstrap-servers: localhost:9192
    properties:
      spring:
        json:
          trusted:
            packages: "*"
  redis:
    port: 6379
    host: localhost
